import styled from "styled-components";

export const Container = styled.div`
  h1 {
    color: var(--dark-gray);
    margin-bottom: 3rem;
    text-align: center;
  }

  button {
    width: 100%;
    color: #fff;
    background: var(--blue-light);
  }
`;
